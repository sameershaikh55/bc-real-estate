import React from "react";

// COMPONENTS
import Layout from "../layout";
import Hero from "../components/Hero";

const Home = () => {
  return (
    <Layout>
      <Hero />
    </Layout>
  );
};

export default Home;
